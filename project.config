[access]
	inheritFrom = All-Projects
[access "refs/*"]
	owner = group user/cagood (cagood)
[access "refs/for/refs/*"]
	read = group user/cagood (cagood)
	push = group user/cagood (cagood)
	label-Code-Review = -2..+2 group user/cagood (cagood)
	label-Verified = -1..+1 group user/cagood (cagood)
	rebase = group user/cagood (cagood)
	removeReviewer = group user/cagood (cagood)
	submit = group user/cagood (cagood)
[access "refs/heads/*"]
	read = group user/cagood (cagood)
	create = group user/cagood (cagood)
	push = +force group user/cagood (cagood)
	pushMerge = group user/cagood (cagood)
[access "refs/tags/*"]
	push = +force group user/cagood (cagood)
	pushTag = group user/cagood (cagood)
	pushSignedTag = group user/cagood (cagood)
